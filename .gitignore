.DS_Store
# Local upstream checkout and build output (scripts/build-local-mac.sh)
.work/

# Local SOC HAR captures (contain live session tokens — never commit)
*.har

# Local docs: install guide and SOC design notes. They name internal
# systems, so they stay on this machine and out of the public repo.
docs/
